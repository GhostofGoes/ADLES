vmware-vsphere:
    hostname: "192.168.0.20"
    port: 443
    login-file: "examples/users.json"
    datacenter: "datacenter"
    datastore: "Datastore"
    template-folder: "Templates/"
    server-root: "root_folder"
    vswitch: "test_vswitch"

docker:
    url: "unix:///var/run/docker.sock"
